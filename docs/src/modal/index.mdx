import {
  Meta,
  Story,
  Canvas,
  Controls,
  Primary,
  Markdown,
} from '@storybook/blocks';

<Meta title="Components/Modal" />

# Modal

<br/>


## API

아래 컴포넌트들을 적절히 조합하여 사용합니다.
각 컴포넌트별 docs를 참조해주세요.

`ModalContentItem` 는 FlexBox,
`ModalHeading`, `ModalSummary`, `ModalDescription` 은 Typography의 props를 동일하게 받습니다.

### `Modal` options

<table width="100%">
  <colgroup>
    <col width="auto" />
    <col width="auto" />
    <col width="auto" />
  </colgroup>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Default</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>open</td>
      <td>boolean</td>
      <td>false</td>
    </tr>
    <tr>
      <td>defaultOpen</td>
      <td>boolean</td>
      <td>false</td>
    </tr>
		<tr>
      <td>onOpenChange</td>
      <td>(open: boolean) => void</td>
      <td></td>
    </tr>
		<tr>
      <td>disableDimmer</td>
      <td>boolean</td>
      <td>false</td>
    </tr>
    <tr>
      <td>disableOutsideClickClose</td>
      <td>boolean</td>
      <td>false</td>
    </tr>
    <tr>
      <td>disableEscapeKeyDownClose</td>
      <td>boolean</td>
      <td>false</td>
    </tr>
		<tr>
      <td>container</td>
      <td>Element</td>
      <td>document.body</td>
    </tr>
  </tbody>
</table>

<Markdown>
{`
\`\`\`tsx
const Component = () => {
  return (
    <Modal open>
      <ModalContainer>
        <ModalNavigation />

        <ModalContent>
          <ModalContentItem>
            <ModalHeading />
            <ModalSummary />
            <ModalDescription />
          </ModalContentItem>
        </ModalContent>

        </ModalActionArea>
          <ModalActionButton />
        </ModalActionArea>
      </ModalContainer>
    </Modal>
  );
};
\`\`\`
`}
</Markdown>

### 여러 콘텐츠

<Markdown>
{`
\`\`\`tsx
const Component = () => {
  return (
    <Modal open>
      <ModalContainer>
        <ModalNavigation />

        <ModalContent>
          <ModalContentItem>
            <ModalHeading />
            <ModalSummary />
            <ModalDescription />
          </ModalContentItem>

          <ModalContentItem>
            <Image />
          </ModalContentItem>
        </ModalContent>

        </ModalActionArea>
          <ModalActionButton />
        </ModalActionArea>
      </ModalContainer>
    </Modal>
  );
};
\`\`\`
`}
</Markdown>