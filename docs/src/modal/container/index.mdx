import {
  Meta,
  Story,
  Canvas,
  Controls,
  Primary,
  Markdown,
} from '@storybook/blocks';
import * as Stories from './index.stories';

<Meta of={Stories} />

# Modal Container

<Primary />

<Controls />

## Responsive

가능한 옵션: `'padding' | 'extra' | 'info' | 'css'`

`xs`, `sm`, `md`, `lg` 를 이용해 반응형 대응을 할 수 있습니다.
