---
title: Navigation
description: Navigation for WDS Utility
---

# Navigation

네비게이션에서 사용하는 테마를 관리합니다.

### Theme 사용

```ts
import { css } from '@emotion/react';

import type { Theme } from '@emotion/react';

export const exampleStyle = (theme: Theme) => css`
  ${theme.platform.ios.navigation}
`;
```

위 코드는 아래와 같이 렌더링 됩니다.

```css
.wds-exampleStyle1 {
  background-color: rgba(var(--palette-background-elevated-normal-rgb), 0.74);
  backdrop-filter: blur(32px);
}
```
