---
title: Sx Prop
description: Sx Prop for WDS
---

# Sx Prop

## 활용

- 디자인시스템 테마 객체에 접근할 수 있는 있어요.
- emotion 종속성 없이도 emotion과 같은 DX를 누릴 수 있어요.
- 모든 디자인시스템 컴포넌트에서 활용이 가능해요.

## Css

<Demo
  code={`import { Box, css, FlexBox } from '@wanteddev/wds';

const widthStyle = css\`
  width: 50px;
  height: 50px;
\`;

const Demo = () => {
  return (
    <FlexBox>
      <Box sx={css\`
        background-color: black;
        \${widthStyle}
      \`}
      />
      <Box
        sx={theme => ({
          backgroundColor: theme.semantic.background.elevated.alternative, width: '50px', height: '50px'
        })}
      />
      <Box sx={theme => css\`
        background-color: \${theme.semantic.primary.normal};
        width: 50px;
        height: 50px;
      \`}
      />
      <Box sx={[
        theme => css\`
          background-color: \${theme.semantic.label.normal};
        \`,
        {
          width: '50px',
          height: '50px',
        }
      ]}
      />
    </FlexBox>
  );
}

export default Demo;
`}
/>


## Keyframes

<Demo
  code={`import { Box, css, keyframes } from '@wanteddev/wds';

const bounce = keyframes\`
  from, 20%, 53%, 80%, to {
    transform: translate3d(0,0,0);
  }

  40%, 43% {
    transform: translate3d(0, -30px, 0);
  }

  70% {
    transform: translate3d(0, -15px, 0);
  }

  90% {
    transform: translate3d(0,-4px,0);
  }
\`;

const Demo = () => {
  return <Box sx={css\`
    background-color: black;
    animation: \${bounce} 1s ease infinite;
    width: 50px;
    height: 50px;
  \`}
  />
}

export default Demo;
`}
/>

## ClassNames

[Emotion](https://emotion.sh/docs/class-names) 의 문서를 참고해주세요.

## 기타 활용

- [container](/wds/docs/utility/container)
- [gradient](/wds/docs/utility/gradient)
- [opacity](/wds/docs/utility/opacity)
- [media](/wds/docs/utility/media)
- [navigation](/wds/docs/utility/navigation)
- [typography](/wds/docs/utility/typography)
