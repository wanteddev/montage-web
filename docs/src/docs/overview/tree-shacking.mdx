---
title: Tree Shacking
description: Tree Shacking Guide for WDS
---

# Tree Shacking

현재 `@wanteddev/wds-icon` 패키지만 tree shacking을 지원합니다.

## Next.js

Nextjs를 사용하는 경우에는 간단하게 트리쉐이킹을 활성화 할 수 있습니다.

```javascript
/** @type {import('next').NextConfig} */
module.exports = {
  experimental: {
    optimizePackageImports: ['@wanteddev/wds-icon'],
  },
};
```

## Compiler

그 외의 경우에는 컴파일러의 도움을 받아 트리쉐이킹을 진행합니다.

### Babel

`pnpm`, `yarn`, `npm` 등 툴을 활용하여 패키지를 설치합니다.

```bash
yarn add -D babel-plugin-import
```

babelrc를 아래와 같이 구성합니다.

```js
module.exports = {
  plugins: [
    ["import", {
      libraryName: "@wanteddev/wds-icon",
      libraryDirectory: "",
      customName: (name) => {
        return `${name.replace('Icon', '').replace(/([a-z0–9])([A-Z])/g, "$1-$2").toLowerCase()}`
      }
    }]
  ]
}
```

### Swc

준비 중입니다.