---
title: Label
description: Label Component
---

# Label

기본 디자인이 적용된 `label` 요소 입니다.
주로 해당 컴포넌트 대신 `FormLabel` 으로 사용합니다.

## 활용

<Demo code={`import { Label } from '@wanteddev/wds';

const Demo = () => {
  return (
    <Label>라벨</Label>
  )
}

export default Demo;`}
/>

## Props

`Tyopgraphy<'label'>` 과 props를 동일하게 사용합니다.

<PropsTable component="Label" />
