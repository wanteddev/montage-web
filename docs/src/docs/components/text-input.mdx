---
title: TextInput
description: TextInput Component
---

# TextInput

사용자가 특정한 텍스트를 입력하거나 편집할 수 있습니다.

## 활용

- `type="date"`, `type="file"` 등 텍스트/숫자가 아닌 요소를 입력받을 때는 사용하지 않아요.
- rightContent, leftContent 요소는 `TextInputContent` 로 래핑해서 사용해요.

<Demo code={`import { TextInput } from '@wanteddev/wds';

const Demo = () => {
  return (
    <TextInput placeholder="입력하세요." width="300px" />
  )
}

export default Demo;`}
/>

## Props

### TextInput

<PropsTable component="TextInput" />

### TextInputContent

<PropsTable component="TextInputContent" />

### TextInputButton

<PropsTable component="TextInputButton" />

## Example

### Icon

- 좌/우측에 요소를 추가할 수 있어요.
- 간격 보정을 위해 `TextInputContent` 로 래핑해서 사용해요.

아래 예시 코드를 확인 해주세요.

<Demo code={`import { TextInput, FlexBox, TextInputContent, IconButton, ContentBadge } from '@wanteddev/wds';
import { IconSearch, IconEye } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent variant="text">
            텍스트
          </TextInputContent>
        }
      />
      
      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent variant="badge">
            <ContentBadge size="medium">
              적절
            </ContentBadge>
          </TextInputContent>
        }
      />

      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent variant="timer">
            03:00
          </TextInputContent>
        }
      />
      
      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent variant="timer" color="palette.status.negative">
            00:00
          </TextInputContent>
        }
      />

      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent variant="icon">
            <IconSearch />
          </TextInputContent>
        }
      />

      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent>
            <IconButton type="button" size={22}>
              <IconEye />
            </IconButton>
          </TextInputContent>
        }
      />

      <TextInput
        placeholder="입력하세요."
        width="300px"
        rightContent={
          <TextInputContent variant="custom">
            커스텀
          </TextInputContent>
        }
      />
    </FlexBox>
  )
}

export default Demo;`}
/>

### TextInputButton

```css
.textfield {
  // bad
  border-radius: 12px 12px 0px 0px;
}

.textfield {
  // good
  border-top-left-radius: 0px;
  border-bottom-left-radius: 0px;
}
```

TextInput의 border radius 2부분을 커스텀 하여서 함께 사용합니다.
`border-top-right-radius` 이런식의 명확한 코너의 radius를 0으로 설정해야합니다.


<Demo code={`import { TextInput, FlexBox, TextInputButton } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <FlexBox>
        <TextInput
          placeholder="입력하세요."
          width="300px"
          sx={{ borderTopRightRadius: 0, borderBottomRightRadius: 0, }}
        />
        <TextInputButton>
          텍스트
        </TextInputButton>
      </FlexBox>

      <FlexBox>
        <TextInputButton position="left">
          텍스트
        </TextInputButton>
        <TextInput
          placeholder="입력하세요."
          width="300px"
          sx={{ borderTopLeftRadius: 0, borderBottomLeftRadius: 0, }}
        />
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>


### Status

오류가 있는 invalid 상태와 유효성 검사를 통과한 positive 상태가 존재합니다.
사용하는 쪽에서 직접 prop으로 주입해서 사용합니다.

<Demo code={`import { TextInput, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <TextInput
        placeholder="입력하세요."
        width="300px"
        invalid
      />

      <TextInput
        placeholder="입력하세요."
        width="300px"
        positive
      />
    </FlexBox>
  )
}

export default Demo;`}
/>

### Form

<Demo code={`import { Button, FlexBox, TextInput, FormField, FormControl, FormLabel, FormErrorMessage } from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <FlexBox
      as="form"
      flexDirection="column"
      gap="20px"
      onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
    >
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
            value: true,
            message: "필수 값입니다.",
          },
        }}
        render={({ field, formState }) => (
          <FormField>
            <FormLabel required>내용</FormLabel>

            <FormControl>
              <TextInput
                {...field}
                placeholder="입력하세요."
                width="300px"
                invalid={Boolean(formState.errors.content)}
              />
            </FormControl>

            <FormErrorMessage>{formState.errors.content?.message?.toString()}</FormErrorMessage>
          </FormField>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}
/>

