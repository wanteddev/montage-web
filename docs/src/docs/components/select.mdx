---
title: Select
description: Select Component
---

# Select

필드를 눌러 드롭다운 메뉴, 모달, 바텀시트를 호출하여 항목을 선택할 수 있습니다.

## 활용

- 여러 옵션 중 하나의 값을 선택할 수 있어요.
- leftContent에 요소를 추가 할 때는 `SelectContent`로 감싸서 사용합니다.

<Demo code={`import { Select, Option, OptionGroup, Label, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <Label>Label</Label>
      <Select width="300px" placeholder="선택해주세요.">
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}
      </Select>
    </FlexBox>
  )
}

export default Demo;`}
/>

## Components

아래와 같이 조합하여 사용합니다.

```tsx
<Select>
  <OptionGroup>
    <Option>
      옵션1
    </Option>
  </OptionGroup>
  <OptionGroup>
    <Option>
      옵션2
    </Option>
  </OptionGroup>
</Select>
```

### Select

<PropsTable component="Select" />

### SelectContent

`TextInputContent`와 동일한 props를 사용합니다.

<PropsTable component="SelectContent" />

### OptionGroup

`MenuGroup`과 동일한 props를 사용합니다.

<PropsTable component="OptionGroup" />

### Option

`MenuItem`과 동일한 props를 사용합니다.

<PropsTable component="Option" />

## Examples

### Radio

`Option` 컴포넌트에 variant="radio" 옵션을 주어 radio를 렌더링 할 수 있습니다.

<Demo code={`import { Select, Option, OptionGroup, Label, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <Label>Label</Label>
      <Select width="300px" placeholder="선택해주세요.">
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option variant="radio" key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}
      </Select>
    </FlexBox>
  )
}

export default Demo;`}
/>

### Invalid

<Demo code={`import { Select, Option, OptionGroup, Label, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <Label>Label</Label>
      <Select invalid width="300px" placeholder="선택해주세요.">
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option variant="radio" key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}
      </Select>
    </FlexBox>
  )
}

export default Demo;`}
/>

### Disabled

<Demo code={`import { Select, Option, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <Select disabled width="300px" placeholder="선택해주세요.">
        <Option value="">값</Option>
      </Select>
      <Select disabled value="값" width="300px" placeholder="선택해주세요.">
        <Option value="값">값</Option>
      </Select>
    </FlexBox>
  )
}

export default Demo;`}
/>

### LeftContent

<Demo code={`import { Select, Option, OptionGroup, SelectContent, FlexBox, IconButton } from '@wanteddev/wds';
import { IconRefresh, IconApps } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <Select
        width="300px"
        placeholder="선택해주세요."
        leftContent={
          <SelectContent variant="icon">
            <IconRefresh />
          </SelectContent>
        }
      >
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}
      </Select>
      <Select
        width="300px"
        placeholder="선택해주세요."
        leftContent={
          <SelectContent variant="icon-button">
            <IconButton type="button">
              <IconApps />
            </IconButton>
          </SelectContent>
        }
      >
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}
      </Select>
      <Select
        width="300px"
        placeholder="선택해주세요."
        leftContent={
          <SelectContent variant="custom">
            커스텀
          </SelectContent>
        }
      >
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}
      </Select>
    </FlexBox>
  )
}

export default Demo;`}
/>

### With Bottom

bottom 영역을 사용하신다면 `enableMenuBottom` prop 을 활용해야합니다.

<Demo code={`import { Typography, Select, Option, OptionGroup, SelectContent, Label, FlexBox, ellipsisTypographyStyle, MenuBottom, MenuBottomContent, Button } from '@wanteddev/wds';
import { IconRefresh } from '@wanteddev/wds-icon';
import { useState } from 'react';

const Demo = () => {
  const [value, setValue] = useState('');
  const [menuValue, setMenuValue] = useState('');
  const [open, setOpen] = useState(false);

  return (
    <FlexBox flexDirection="column" gap="8px">
      <Label>Label</Label>
      <Select
        enableMenuBottom
        open={open}
        onOpenChange={setOpen}
        value={value}
        onValueChange={setValue}
        menuValue={menuValue}
        onMenuValueChange={setMenuValue}
        width="300px"
        placeholder="선택해주세요."
      >
        {new Array(3).fill(0).map((_, i) => (
          <OptionGroup key={i} title={\`그룹 \${i + 1}\`}>
            {new Array(3).fill(0)
              .map((__, j) => (
                <Option key={j} value={\`값 \${i} \${j}\`}>
                  {\`값 \${i} \${j}\`}
                </Option>
              ))
            }
          </OptionGroup>
        ))}

        <MenuBottom
          leftContent={
            <MenuBottomContent variant="button">
              <Button
                variant="outlined"
                color="assistive"
                iconOnly
                size="small"
                onClick={() => setMenuValue('')}
              >
                <IconRefresh />
              </Button> 
            </MenuBottomContent>
          }
          rightContent={
            <MenuBottomContent variant="button">
              <Button size="small" onClick={() => {
                setValue(menuValue);
                setOpen(false);
              }}>
                적용
              </Button>
            </MenuBottomContent>
          }
        />
      </Select>
    </FlexBox>
  )
}

export default Demo;`}
/>
