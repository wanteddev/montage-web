---
title: Card
description: Card Component
---

# Card

- 일반적인 상황에서 정보를 묶어 표시할 때 사용합니다.
- leftContent, rightContent에 요소를 추가 할 때는 `CardThumbnailContent` 로 감싸주세요.

<Demo code={`import { FlexBox, Card, CardThumbnail, CardTitle, CardCaption, CardContent, CardThumbnailContent } from '@wanteddev/wds';
import { IconBookmark } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <FlexBox gap="24px" justifyContent="center">
      <Card width="240px">
        <CardThumbnail
          src="https://static.wanted.co.kr/images/company/79/elpzxpmgh94xesrf__1080_790.png"
          alt="Wanted Company Image"
          width="240px"
          quality={95}
          overlay
          leftContent={
            <CardThumbnailContent variant="text">
              캡션
            </CardThumbnailContent>
          }
          rightContent={
            <CardThumbnailContent variant="icon">
              <IconBookmark />
            </CardThumbnailContent>
          }
        />
        <CardContent>
          <CardTitle>제목</CardTitle>
          <CardCaption>캡션</CardCaption>
        </CardContent>
      </Card>
      <Card platform="mobile" width="152px">
        <CardThumbnail
          src="https://static.wanted.co.kr/images/company/79/elpzxpmgh94xesrf__1080_790.png"
          alt="Wanted Company Image"
          width="152px"
          quality={95}
          overlay
          leftContent={
            <CardThumbnailContent variant="text">
              캡션
            </CardThumbnailContent>
          }
          rightContent={
            <CardThumbnailContent variant="icon">
              <IconBookmark />
            </CardThumbnailContent>
          }
        />
        <CardContent>
          <CardTitle>제목</CardTitle>
          <CardCaption>캡션</CardCaption>
        </CardContent>
      </Card>
    </FlexBox>
  );
}

export default Demo;`}
/>

## Components

아래와 같이 조합하여 사용합니다.

```tsx
<Card>
  <CardThumbnail />
  <CardContent>
    <CardExtraContent/>
    <CardTitle />
    <CardCaption />
    <CardExtraContent />
  </CardContent>
</Card>
```

### Card

<PropsTable component="Card" />

### CardThumbnail

<PropsTable component="CardThumbnail" />

### CardThumbnailContent

<PropsTable component="CardThumbnailContent" />

### CardContent

<PropsTable component="CardContent" />

### CardExtraContent

<PropsTable component="CardExtraContent" />

### CardTitle

`Typography`와 동일한 props를 사용해요.

<PropsTable component="CardTitle" />

### CardCaption

`Typography`와 동일한 props를 사용해요.

<PropsTable component="CardCaption" />

## Examples

### Top Content, Bottom Content

- `CardExtraContent`의 `position`을 조정하여 top, bottom에 콘텐츠를 추가할 수 있어요.
- 뱃지나 부가적인 내용을 추가할 때 사용해요.

<Demo code={`import { FlexBox, Card, CardThumbnail, CardTitle, CardCaption, CardContent, CardExtraContent, ContentBadge } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox gap="24px" justifyContent="center">
      <Card width="240px">
        <CardThumbnail src="https://" width="240px" />
        <CardContent>
          <CardExtraContent position="top" variant="badge">
            {new Array(3).fill(0).map((_, i) => (
              <ContentBadge key={i} color="neutral">텍스트</ContentBadge>
            ))}
          </CardExtraContent>
          <CardTitle>제목</CardTitle>
          <CardCaption>캡션</CardCaption>
        </CardContent>
      </Card>
      <Card width="240px">
        <CardThumbnail src="https://" width="240px" />
        <CardContent>
          <CardTitle>제목</CardTitle>
          <CardCaption>캡션</CardCaption>
          <CardExtraContent position="bottom" variant="badge">
            {new Array(3).fill(0).map((_, i) => (
              <ContentBadge key={i} color="neutral">텍스트</ContentBadge>
            ))}
          </CardExtraContent>
        </CardContent>
      </Card>
    </FlexBox>
  );
}

export default Demo;`}
/>

### Skeleton