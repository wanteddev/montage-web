---
title: List
description: List Component
---

# List

인터랙션이 가능한 리스트입니다.

## 활용

- ListItem, ListItem 을 클릭 시 자동으로 leftContent, rightContent 내부의 컨트롤 요소를 클릭 처리 합니다.
  - 단 switch 컴포넌트는 클릭 처리 하지 않습니다.
- leftContent, rightContent 에 요소를 추가 할 때는 `ListItemContent` 로 감싸서 사용합니다.

<Demo code={`import { List, ListItem, ListText, ListItemContent, Checkbox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <List>
      <ListItem leftContent={(
        <ListItemContent variant="checkbox">
          <Checkbox />
        </ListItemContent>
      )}>
        <ListText>텍스트</ListText>
      </ListItem>
      <ListItem leftContent={(
        <ListItemContent variant="checkbox">
          <Checkbox />
        </ListItemContent>
      )}>
        <ListText caption="캡션">텍스트</ListText>
      </ListItem>
    </List>
  )
}

export default Demo;`}
/>


## Components

아래와 같이 조합하여 사용합니다.

```tsx
<List>
  <ListItem>
    <ListText>리스트1</ListText>
  </ListItem>
  <ListItem>
    <ListText>리스트2</ListText>
  </ListItem>
</List>
```

### List

`FlexBox`와 동일한 props를 사용해요.

<PropsTable component="List" />

### ListItem

<PropsTable component="ListItem" />

### ListItemContent

<PropsTable component="ListItemContent" />

### ListText

<PropsTable component="ListText" />

## Examples

### ListItem

ListItem 대신 ListItem을 사용할 수 있어요. ListItem과는 다르게 인터렉션 영역, padding이 존재하지 않아요.

<Demo code={`import { List, ListItem, ListText } from '@wanteddev/wds';

const Demo = () => {
  return (
    <List gap="16px">
      <ListItem>
        <ListText>텍스트</ListText>
      </ListItem>
      <ListItem active>
        <ListText>텍스트, active</ListText>
      </ListItem>
      <ListItem disabled>
        <ListText>텍스트, disabled</ListText>
      </ListItem>
    </List>
  )
}

export default Demo;`}
/>


### Padding, Active

padding, fillWidth으로 간격을 조정하고, active, disabled 로 상태를 표시해요.

<Demo code={`import { List, ListItem, ListText } from '@wanteddev/wds';

const Demo = () => {
  return (
    <List>
      <ListItem fillWidth>
        <ListText>텍스트, fillWidth</ListText>
      </ListItem>
      <ListItem padding="0px">
        <ListText>텍스트, 0px</ListText>
      </ListItem>
      <ListItem>
        <ListText>텍스트, 12px</ListText>
      </ListItem>
      <ListItem padding="8px">
        <ListText>텍스트, 8px</ListText>
      </ListItem>
      <ListItem padding="16px">
        <ListText>텍스트, 16px</ListText>
      </ListItem>
      <ListItem active>
        <ListText>텍스트, active</ListText>
      </ListItem>
      <ListItem disabled>
        <ListText>텍스트, disabled</ListText>
      </ListItem>
      <ListItem divider>
        <ListText>텍스트, divider</ListText>
      </ListItem>
    </List>
  )
}

export default Demo;`}
/>

### LeftContent, RightContent

- 기본적으로 leftContent, rightContent는 `ListItemContent` 로 래핑해서 사용해요.
- height로 콘텐츠 높이를 조정할 수 있어요.

<Demo code={`import { RadioGroupItem, Checkbox, Thumbnail, Avatar, List, ListItem, ListText, ListItemContent, IconButton, NestedCheckbox, TextButton, ContentBadge, Switch } from '@wanteddev/wds';
import { IconBlank } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <List gap="16px">
      <ListItem
        rightContent={
          <ListItemContent variant="icon">
            <IconBlank />
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent variant="checkbox">
            <NestedCheckbox />
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent variant="button">
            <TextButton size="small" variant="assistive">
              버튼
            </TextButton>
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent>
            <ContentBadge color="neutral">뱃지</ContentBadge>
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent variant="chevron">
            값
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent variant="chevron" />
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent variant="chevron" chevron={false}>
            값
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        leftContent={
          <ListItemContent variant="icon">
            <IconBlank />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="chevron" chevron={false}>
            값
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>
      <ListItem
        leftContent={
          <ListItemContent variant="checkbox">
            <Checkbox />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="chevron" chevron={false}>
            값
          </ListItemContent>
        }
      >
        <ListText>텍스트, Normal</ListText>
      </ListItem>

      <ListItem
        leftContent={
          <ListItemContent height="medium" variant="avatar">
            <Avatar variant="person" size="medium" />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="chevron" chevron={false}>
            값
          </ListItemContent>
        }
      >
        <ListText>텍스트, Medium</ListText>
      </ListItem>
      <ListItem
        alignItems="center"
        leftContent={
          <ListItemContent height="medium" variant="avatar">
            <Avatar variant="person" size="medium" />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="checkbox">
            <NestedCheckbox />
          </ListItemContent>
        }
      >
        <ListText>텍스트, Medium</ListText>
      </ListItem>
      <ListItem
        rightContent={
          <ListItemContent height="medium" variant="switch">
            <Switch />
          </ListItemContent>
        }
      >
        <ListText>텍스트, Medium</ListText>
      </ListItem>

      <ListItem
        leftContent={
          <ListItemContent height="large" variant="large-icon">
            <IconBlank />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="chevron" chevron={false}>
            값
          </ListItemContent>
        }
      >
        <ListText caption="캡션">텍스트, Large</ListText>
      </ListItem>
      <ListItem
        alignItems="center"
        leftContent={
          <ListItemContent height="large" variant="large-icon">
            <IconBlank />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="checkbox">
            <NestedCheckbox />
          </ListItemContent>
        }
      >
        <ListText caption="캡션">텍스트, Large</ListText>
      </ListItem>
      <ListItem
        leftContent={
          <ListItemContent height="large" variant="custom">
            <Thumbnail
              src="https://static.wanted.co.kr/images/company/79/elpzxpmgh94xesrf__1080_790.png"
              alt="Wanted Company Image"
              width="400px"
              ratio="1:1"
              quality={95}
              sx={{ width: '56px' }}
            />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="checkbox">
            <NestedCheckbox />
          </ListItemContent>
        }
      >
        <ListText caption="캡션">텍스트, Large</ListText>
      </ListItem>
    </List>
  )
}

export default Demo;`}
/>

### Ellipsis

- ellipsis가 false일 때 수직 정렬은 상단으로 조정 됩니다.
- ellipsis가 true일 때 수직 정렬은 중앙으로 조정 됩니다.

<Demo code={`import { List, ListItem, ListText, ListItemContent } from '@wanteddev/wds';
import { IconBlank } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <List gap="16px" sx={{ width: '50%' }}>
      <ListItem
        leftContent={
          <ListItemContent height="large" variant="large-icon">
            <IconBlank />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="chevron">
            값
          </ListItemContent>
        }
      >
        <ListText 
          caption="캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션
            캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션"
        >
          텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트
          텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트
          텍스트텍스트텍스트텍스트
        </ListText>
      </ListItem>
      <ListItem
        ellipsis
        leftContent={
          <ListItemContent height="large" variant="large-icon">
            <IconBlank />
          </ListItemContent>
        }
        rightContent={
          <ListItemContent variant="chevron">
            값
          </ListItemContent>
        }
      >
        <ListText 
          caption="캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션
            캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션캡션"
        >
          텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트
          텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트텍스트
          텍스트텍스트텍스트텍스트
        </ListText>
      </ListItem>
    </List>
  )
}

export default Demo;`}
/>

### Checkbox

<Demo code={`import { List, Checkbox, ListItem, ListItemContent, ListText } from '@wanteddev/wds';

const Demo = () => {
  return (
    <List gap="16px">
      <ListItem
        leftContent={
          <ListItemContent variant="checkbox">
            <Checkbox />
          </ListItemContent>
        }
      >
        <ListText>Man</ListText>
      </ListItem>
      <ListItem
        leftContent={
          <ListItemContent variant="checkbox">
            <Checkbox />
          </ListItemContent>
        }
      >
        <ListText>Woman</ListText>
      </ListItem>
    </List>
  )
}

export default Demo;`}
/>

### Form

checkbox를 활용한 form은 아래와 같이 사용합니다.

<Demo code={`import { Button, FlexBox, Checkbox, FormField, FormControl, FormLabel, FormErrorMessage, List, ListItem, ListItemContent, ListText } from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const items = [
  {
    id: "develop",
    label: "개발",
  },
  {
    id: "design",
    label: "디자인",
  },
];

const Demo = () => {
  const form = useForm({
    defaultValues: {
      selected: [],
    },
  });

  return (
    <FlexBox
      as="form"
      flexDirection="column"
      gap="20px"
      onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
    >
      <Controller
        control={form.control}
        name="selected"
        render={({ formState }) => (
          <FormField>
            <FormLabel>관심 분야</FormLabel>

            <List>
              {items.map((item) => (
                <Controller
                  key={item.id}
                  control={form.control}
                  name="selected"
                  rules={{
                    required: {
                      value: true,
                      message: "필수 값입니다.",
                    },
                  }}
                  render={({ field }) => (
                    <ListItem
                      as={FormField}
                      flexDirection="row"
                      alignItems="center"
                      leftContent={
                        <FormControl>
                          <ListItemContent variant="checkbox">
                            <Checkbox
                              {...field}
                              checked={field.value.includes(item.id)}
                              onCheckedChange={(checked) => {
                                return checked
                                  ? field.onChange([...field.value, item.id])
                                  : field.onChange(
                                      field.value.filter(
                                        (value) => value !== item.id
                                      )
                                    );
                              }}
                            />
                          </ListItemContent>
                        </FormControl>
                      }
                    >
                      <ListText>{item.label}</ListText>
                    </ListItem>
                  )}
                />
              ))}
            </List>

            <FormErrorMessage>{formState.errors.selected?.message}</FormErrorMessage>
          </FormField>
        )}
      />
      <Button type="submit">제출</Button>
    </FlexBox>
  );
};

export default Demo;`}
/>

Radio를 사용할 때는 `RadioGroup` 으로 감싸서 사용합니다.

<Demo code={`import { Button, FlexBox, RadioGroup, RadioGroupItem, FormField, FormControl, List, ListItem, ListItemContent, ListText, FormLabel, FormErrorMessage } from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <FlexBox
      as="form"
      flexDirection="column"
      gap="20px"
      onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
    >
      <Controller
        control={form.control}
        name="selected"
        rules={{
          required: {
            value: true,
            message: "필수 값입니다.",
          },
        }}
        render={({ field, formState }) => (
          <FormField>
            <FormLabel>성별</FormLabel>

            <FormControl>
              <RadioGroup {...field} onValueChange={field.onChange}>
                <List>
                  <ListItem
                    as={FormField}
                    leftContent={
                      <FormControl>
                        <ListItemContent variant="radio">
                          <RadioGroupItem value="man" />
                        </ListItemContent>
                      </FormControl>
                    }
                  >
                    <ListText>Man</ListText>
                  </ListItem>
                  <ListItem
                    as={FormField}  
                    leftContent={
                      <FormControl>
                        <ListItemContent variant="radio">
                          <RadioGroupItem value="woman" />
                        </ListItemContent>
                      </FormControl>
                    }
                  >
                    <ListText>Woman</ListText>
                  </ListItem>
                </List>
              </RadioGroup>
            </FormControl>

            <FormErrorMessage>{formState.errors.selected?.message}</FormErrorMessage>
          </FormField>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}
/>


Switch를 사용하는 경우 `ListItem` 영역을 클릭해서 토글되지 않습니다.
스위치 영역을 직접 클릭해야만 스위치가 토글됩니다.

`disableInteraction` 과 함께 사용하면 자연스러운 동작으로 사용 가능합니다.

<Demo code={`import { Button, FlexBox, Switch, List, ListItem, ListItemContent, ListText, FormField, FormControl, FormLabel } from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <FlexBox
      as="form"
      flexDirection="column"
      gap="20px"
      onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
    >
      <List>
        <Controller
          control={form.control}
          name="notification"
          render={({ field, formState }) => (
            <ListItem
              as={FormField}
              disableInteraction
              rightContent={
                <FormControl>
                  <ListItemContent variant="switch" height="medium">
                    <Switch
                      {...field}
                      checked={field.value} 
                      onCheckedChange={field.onChange}
                    />
                  </ListItemContent>
                </FormControl>
              }
            >
              <ListText>알림 받기</ListText>
            </ListItem>
          )}
        />
        <Controller
          control={form.control}
          name="darkMode"
          render={({ field, formState }) => (
            <ListItem
              as={FormField}
              disableInteraction
              rightContent={
                <FormControl>
                  <ListItemContent variant="switch" height="medium">
                    <Switch
                      {...field}
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </ListItemContent>
                </FormControl>
              }
            >
              <ListText>다크 모드</ListText>
            </ListItem>
          )}
        />
      </List>
      
      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}
/>