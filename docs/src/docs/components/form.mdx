---
title: Form
description: Form Component
---

# Form

`react-hook-form` 과 함께 form action을 다룹니다.
각 컴포넌트별 활용법은 `TextField`, `TextArea`, `Checkbox`, `Radio` 등을 참고해주세요

## 활용

<Demo code={`import { Button, FlexBox, TextField, Form, FormField, FormItem, FormControl, FormLabel, FormMessage } from '@wanteddev/wds';
import { useForm } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <Form {...form}>
      <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
        <FormField
          control={form.control}
          name="content"
          rules={{
            required: {
              value: true,
              message: '필수 값입니다.'
            }
          }}
          render={({ field }) => (
            <FormItem>
              <FormLabel>라벨</FormLabel>

              <FormControl>
                <TextField {...field} width="300px" placeholder="값을 입력하세요." />
              </FormControl>

              <FormMessage />
            </FormItem>
          )} />

        <Button type="submit">제출</Button>
      </FlexBox>
    </Form>
  )
}

export default Demo;`}/>

## Components

아래와 같이 여러 컴포넌트를 조합하여 사용합니다.

```tsx
<Form {...form}>
  <form onSubmit={handleSubmit}>
    <FormField
      control={form.control}
      name="name"
      render={({ field }) => (
        <FormItem>
          <FormLabel>label</FormLabel>
          <FormControl>
            <input {...field} />
          </FormControl>

          <FormMessage>설명</FormMessage>
          {/* 혹은 */}
          <FormDescription>설명</FormDescription>
          <FormErrorMessage />
        </FormItem>
      )}
    />

    <button type="submit" />
  </form>
</Form>
```

### Form

`react-hook-form` 의 `useForm` 반환값을 그대로 전달합니다.

```tsx
const form = useForm();

return <Form {...form} />
```

### FormField

`react-hook-form` 의 `Controller` 의 역할을 합니다.

<PropsTable component="FormField" />

### FormItem

form 요소를 렌더링하기 위한 form item 입니다.
내부적으로 `FormLabel`, `FormMessage`, `FormControl` 에 접근성 처리를 합니다. 


`FlexBox` 와 같은 props를 사용합니다.

<PropsTable component="FormItem" />

### FormLabel

`Typography<Label>` 과 동일한 Props를 사용합니다.

<PropsTable component="FormLabel" />

### FormControl

children으로 제어 컴포넌트를 넣어 사용합니다.

### FormDescription

`Typography` 과 동일한 Props를 사용합니다.

<PropsTable component="FormDescription" />

### FormErrorMessage

`Typography` 과 동일한 Props를 사용합니다.

<PropsTable component="FormErrorMessage" />

### FormMessage

Form의 오류 메시지를 보여주거나 Form에 대한 설명을 추가합니다.

`Typography` 과 동일한 Props를 사용합니다.

<PropsTable component="FormMessage" />

## Advanced

### FormMessage

`FormMessage` 은 field에 대한 설명과 오류 메시지를 함께 다룹니다.

아래와 같이 오류 메시지가 없을 때는 설명이 나오며, 오류 메시지가 있을 때는 설명을 덮어씌웁니다.

<Demo code={`import { Button, FlexBox, TextField, Form, FormField, FormItem, FormControl, FormLabel, FormMessage } from '@wanteddev/wds';
import { useForm } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <Form {...form}>
      <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
        <FormField
          control={form.control}
          name="content"
          rules={{
            required: {
              value: true,
              message: '필수 값입니다.'
            }
          }}
          render={({ field }) => (
            <FormItem>
              <FormLabel>라벨</FormLabel>

              <FormControl>
                <TextField {...field} width="300px" placeholder="값을 입력하세요." />
              </FormControl>

              <FormMessage>설명</FormMessage>
            </FormItem>
          )} />

        <Button type="submit">제출</Button>
      </FlexBox>
    </Form>
  )
}

export default Demo;`}/>

### FormErrorMessage + FormDescription

`FormMessage` 와는 반대로 각각 ErrorMessage, Description을 사용할 수 있습니다.

<Demo code={`import { Button, FlexBox, TextField, Form, FormField, FormItem, FormControl, FormLabel, FormDescription, FormErrorMessage } from '@wanteddev/wds';
import { useForm } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <Form {...form}>
      <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
        <FormField
          control={form.control}
          name="content"
          rules={{
            required: {
              value: true,
              message: '필수 값입니다.'
            }
          }}
          render={({ field }) => (
            <FormItem>
              <FormLabel>라벨</FormLabel>

              <FormControl>
                <TextField {...field} width="300px" placeholder="값을 입력하세요." />
              </FormControl>


              <FormDescription>설명</FormDescription>
              <FormErrorMessage />
            </FormItem>
          )} />

        <Button type="submit">제출</Button>
      </FlexBox>
    </Form>
  )
}

export default Demo;`}/>

## 자주 겪는 오류

### form.reset

`form.reset()` 이 동작하지 않는 경우가 있습니다.
이 경우 `defaultValue` 가 설정되어 있는지 확인하세요.

[react-hook-form](https://github.com/orgs/react-hook-form/discussions/5858) 은 undefined value를 지원하지 않습니다.
