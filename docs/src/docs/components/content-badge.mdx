---
title: ContentBadge
description: ContentBadge Component
---

# ContentBadge

항목을 분류하거나 내용의 추가 속성을 표시하는 데 사용합니다.

## 활용

- 카드 컴포넌트에서 분류되는 항목을 표시해요.
- 추가 정보를 보여줄 때에 써요.
- 배경과 텍스트 컬러를 섞어 쓰지 않아요.

<Demo code={`import { ContentBadge } from '@wanteddev/wds';

const Demo = () => {
  return (
    <ContentBadge color="neutral">
      원티드
    </ContentBadge>
  )
}

export default Demo;`}
/>

## Props

<PropsTable component="ContentBadge" />

## Variant

### Solid

<Demo code={`import { ContentBadge } from '@wanteddev/wds';

const Demo = () => {
  return (
    <ContentBadge color="neutral" variant="solid">
      원티드
    </ContentBadge>
  )
}

export default Demo;`}
/>


### Outlined

<Demo code={`import { ContentBadge } from '@wanteddev/wds';

const Demo = () => {
  return (
    <ContentBadge color="neutral" variant="outlined">
      원티드
    </ContentBadge>
  )
}

export default Demo;`}
/>

## Color

### Neutral

<Demo code={`import { ContentBadge } from '@wanteddev/wds';

const Demo = () => {
  return (
    <ContentBadge color="neutral">
      원티드
    </ContentBadge>
  )
}

export default Demo;`}
/>

### Accent

`accentColor` 로 뱃지 색상을 수정합니다.

<Demo code={`import { ContentBadge, FlexBox, Typography } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="24px">
      <FlexBox flexDirection="column" gap="12px">
        <Typography>solid</Typography>
        <FlexBox gap="8px">
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.red">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.redOrange">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.orange">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.lime">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.green">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.cyan">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.lightBlue">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.blue">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.violet">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.purple">
            원티드
          </ContentBadge>
          <ContentBadge color="accent" accentColor="semantic.accent.foreground.pink">
            원티드
          </ContentBadge>
        </FlexBox>
      </FlexBox>

      <FlexBox flexDirection="column" gap="12px">
        <Typography>outlined</Typography>
        <FlexBox gap="8px">
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.red">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.redOrange">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.orange">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.lime">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.green">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.cyan">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.lightBlue">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.blue">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.violet">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.purple">
            원티드
          </ContentBadge>
          <ContentBadge variant="outlined" color="accent" accentColor="semantic.accent.foreground.pink">
            원티드
          </ContentBadge>
        </FlexBox>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>
