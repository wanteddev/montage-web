---
title: TextArea
description: TextArea Component
---

# TextArea

사용자가 특정한 텍스트를 입력하거나 편집할 수 있습니다.

## 활용

- 입력한 텍스트에 따라 자동으로 늘어나요.
- `maxRows` 와 `minRows` 로 최대 / 최소로 늘어나는 line 수를 정할 수 있어요.
- padding 값을 변경할 때 유의가 필요해요. `paddingRight`, `paddingBottom`이 자동으로 동적 변환되기 때문이에요.
- 너비 값은 `width` prop을 이용해주세요.

<Demo code={`import { TextArea } from '@wanteddev/wds';

const Demo = () => {
  return (
    <TextArea placeholder="입력하세요." width="300px" />
  )
}

export default Demo;`}
/>

## Props

<PropsTable component="TextArea" />

## Example

### MaxRows

<Demo code={`import { TextArea, FlexBox, Label, TextButton } from '@wanteddev/wds';

const Demo = () => {
  return (
    <TextArea
      placeholder="입력하세요."
      width="300px"
      maxRows={3}
    />
  )
}

export default Demo;`}
/>

### Action

<Demo code={`import { TextArea, FlexBox, Label, TextButton } from '@wanteddev/wds';

const Demo = () => {
  return (
    <TextArea
      placeholder="입력하세요."
      width="300px"
      rightIcon={
        <TextButton size="small">
          보내기
        </TextButton>
      }
    />
  )
}

export default Demo;`}
/>

### Action Icon

<Demo code={`import { TextArea, FlexBox, Label, IconButton } from '@wanteddev/wds';
import { IconSendFill } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <TextArea
      placeholder="입력하세요."
      width="300px"
      rightIcon={
        <IconButton variant="solid" size="small">
          <IconSendFill />
        </IconButton>
      }
    />
  )
}

export default Demo;`}
/>

### Max Length

<Demo code={`import { TextArea, FlexBox, Label, TextButton } from '@wanteddev/wds';

const Demo = () => {
  return (
    <TextArea
      placeholder="입력하세요."
      width="300px"
      maxLength={30}
    />
  )
}

export default Demo;`}
/>

### Form

<Demo code={`import { Button, FlexBox, TextArea, FormField, FormControl, FormLabel, FormErrorMessage } from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <FlexBox
      as="form"
      flexDirection="column"
      gap="20px"
      onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
    >
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
            value: true,
            message: "필수 값입니다.",
          },
        }}
        render={({ field, formState }) => (
          <FormField>
            <FormLabel>내용</FormLabel>

            <FormControl>
              <TextArea
                {...field}
                placeholder="입력하세요."
                width="300px"
                invalid={Boolean(formState.errors.content)}
              />
            </FormControl>

            <FormErrorMessage>{formState.errors.content?.message}</FormErrorMessage>
          </FormField>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}
/>

