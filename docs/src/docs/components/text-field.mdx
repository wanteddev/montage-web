---
title: TextField
description: TextField Component
---

# TextField

사용자가 특정한 텍스트를 입력하거나 편집할 수 있습니다.

## 활용

- `type="date"`, `type="file"` 등 텍스트/숫자가 아닌 요소를 입력받을 때는 사용하지 않아요.
- trailingContent, leadingContent 요소는 `TextFieldContent` 로 래핑해서 사용해요.

<Demo code={`import { TextField } from '@wanteddev/wds';

const Demo = () => {
  return (
    <TextField placeholder="입력하세요." width="300px" />
  )
}

export default Demo;`}
/>

## Props

### TextField

<PropsTable component="TextField" />

### TextFieldContent

<PropsTable component="TextFieldContent" />

### TextFieldButton

<PropsTable component="TextFieldButton" />

## Example

### Icon

- 좌/우측에 요소를 추가할 수 있어요.
- 간격 보정을 위해 `TextFieldContent` 로 래핑해서 사용해요.

아래 예시 코드를 확인 해주세요.

<Demo code={`import { TextField, FlexBox, TextFieldContent, IconButton, ContentBadge } from '@wanteddev/wds';
import { IconSearch, IconEye } from '@wanteddev/wds-icon';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="text">
            텍스트
          </TextFieldContent>
        }
      />
      
      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="badge">
            <ContentBadge size="medium">
              적절
            </ContentBadge>
          </TextFieldContent>
        }
      />

      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="timer">
            03:00
          </TextFieldContent>
        }
      />
      
      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="timer" color="semantic.status.negative">
            00:00
          </TextFieldContent>
        }
      />

      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="icon">
            <IconSearch />
          </TextFieldContent>
        }
      />

      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="icon-button">
            <IconButton type="button" size={22}>
              <IconEye />
            </IconButton>
          </TextFieldContent>
        }
      />

      <TextField
        placeholder="입력하세요."
        width="300px"
        trailingContent={
          <TextFieldContent variant="custom">
            커스텀
          </TextFieldContent>
        }
      />
    </FlexBox>
  )
}

export default Demo;`}
/>

### TextFieldButton

```css
.textfield {
  // bad
  border-radius: 12px 12px 0px 0px;
}

.textfield {
  // good
  border-top-left-radius: 0px;
  border-bottom-left-radius: 0px;
}
```

TextField의 border radius 코너 두 부분을 커스텀 하여서 함께 사용합니다.
`border-top-right-radius` 이런식의 명확한 코너의 radius를 0으로 설정해야합니다.


<Demo code={`import { TextField, FlexBox, TextFieldButton } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <FlexBox>
        <TextField
          placeholder="입력하세요."
          width="300px"
          sx={{ borderTopRightRadius: 0, borderBottomRightRadius: 0, }}
        />
        <TextFieldButton>
          텍스트
        </TextFieldButton>
      </FlexBox>

      <FlexBox>
        <TextFieldButton position="left">
          텍스트
        </TextFieldButton>
        <TextField
          placeholder="입력하세요."
          width="300px"
          sx={{ borderTopLeftRadius: 0, borderBottomLeftRadius: 0, }}
        />
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>


### Status

오류가 있는 invalid 상태와 유효성 검사를 통과한 positive 상태가 존재합니다.
사용하는 쪽에서 직접 prop으로 주입해서 사용합니다.

<Demo code={`import { TextField, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <TextField
        placeholder="입력하세요."
        width="300px"
        invalid
      />

      <TextField
        placeholder="입력하세요."
        width="300px"
        positive
      />
    </FlexBox>
  )
}

export default Demo;`}
/>

### Form

<Demo code={`import { Button, FlexBox, TextField, FormField, FormControl, FormLabel, FormErrorMessage } from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm();

  return (
    <FlexBox
      as="form"
      flexDirection="column"
      gap="20px"
      onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
    >
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
            value: true,
            message: "필수 값입니다.",
          },
        }}
        render={({ field, formState }) => (
          <FormField>
            <FormLabel required>내용</FormLabel>

            <FormControl>
              <TextField
                {...field}
                placeholder="입력하세요."
                width="300px"
                invalid={Boolean(formState.errors.content)}
              />
            </FormControl>

            <FormErrorMessage>{formState.errors.content?.message?.toString()}</FormErrorMessage>
          </FormField>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}
/>

