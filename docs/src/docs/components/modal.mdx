---
title: Modal
description: Modal Component
---

# Modal

## 활용

- Modal Overlay를 표시합니다.
- padding 등을 커스텀 할 땐 css 변수를 활용해주세요.
- `huge` 사이즈는 560px를 초과해서 사용하고 싶을 때 활용해요.
  - 640, 720, 800 같이 80px 단위로 끊어서 사용해요.
- `TopNavigation` 에서 leftButton, rightButton 에는 `TopNavigationButton`, `ModalClose` 를 사용합니다.
- `ModalContainer`, `TopNavigation`, `ModalContent`, `ActionArea` 의 위계를 변경해서는 안됩니다.

<Demo code={`import { useState } from 'react';
import {
  Button,
  Modal,
  ActionArea,
  ActionAreaButton,
  ModalContainer,
  ModalContent,
  ModalContentItem,
  ModalDescription,
  ModalHeading,
  TopNavigation,
  ModalSummary,
} from '@wanteddev/wds';

const Demo = () => {
  const [show, setShow] = useState(false);

  return (
    <>
      <Modal
        open={show}
        onOpenChange={setShow}
      >
        <ModalContainer variant="popup">
          <TopNavigation>
            제목
          </TopNavigation>

          <ModalContent>
            <ModalContentItem>
              <ModalHeading>Heading</ModalHeading>
              <ModalSummary>Summary</ModalSummary>
              <ModalDescription>Description</ModalDescription>
            </ModalContentItem>
          </ModalContent>

          <ActionArea>
            <ActionAreaButton>
              액션
            </ActionAreaButton>
          </ActionArea>
        </ModalContainer>
      </Modal>

      <Button onClick={() => setShow(true)}>Click me</Button>
    </>
  )
}

export default Demo;`}
/>

## Components

Modal 은 여러 컴포넌트를 조합해서 사용합니다.

기본 구성은 아래와 같습니다.

```tsx
<Modal>
  <ModalContainer>
    <TopNavigation />

    <ModalContent>
      <ModalContentItem>
        <ModalHeading />
        <ModalSummary />
        <ModalDescription />
      </ModalContentItem>
    </ModalContent>

    <ActionArea>
      <ActionAreaButton />
    </ActionArea>
  </ModalContainer>
</Modal>
```

### Modal

<PropsTable component="Modal" />

### ModalContainer

<PropsTable component="ModalContainer" />

### ModalClose

`TopNavigationButton` 과 동일한 Props를 사용합니다.

### ModalContent

<PropsTable component="ModalContent" />

### ModalContentItem

`FlexBox` 와 동일한 Props를 사용합니다.

### ModalHeading

`Typography` 와 동일한 Props를 사용합니다.

### ModalSummary

`Typography` 와 동일한 Props를 사용합니다.

### ModalDescription

`Typography` 와 동일한 Props를 사용합니다.

## Examples

### Form

<Demo code={`import { useState } from 'react';
import {
  Button,
  Modal,
  ActionArea,
  ActionAreaButton,
  ModalContainer,
  ModalContent,
  ModalContentItem,
  TopNavigation,
  TextField,
  FlexBox,
  TextField,
  FormField,
  FormControl,
  FormLabel,
  FormErrorMessage,
  FormMessage,
} from '@wanteddev/wds';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const [show, setShow] = useState(false);
  const form = useForm({ defaultValues: { test: '' } });

  return (
    <>
      <Modal
        open={show}
        onOpenChange={setShow}
      >
        <ModalContainer variant="popup">
          <FlexBox
            as="form"
            flex="1"
            flexDirection="column"
            onSubmit={form.handleSubmit((v) => alert(JSON.stringify(v)))}
          >
            <TopNavigation
              variant="emphasized"
            >
              제목
            </TopNavigation>

            <ModalContent padding paddingInfo>
              <ModalContentItem>
                <Controller
                  control={form.control}
                  name="test"
                  rules={{
                    required: {
                      value: true,
                      message: '필수 값입니다.'
                    }
                  }}
                  render={({ field, formState }) => (
                    <FormField>
                      <FormLabel>라벨</FormLabel>

                      <FormControl>
                        <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.test)} />
                      </FormControl>

                      <FormMessage>설명</FormMessage>
                      <FormErrorMessage>{formState.errors.test?.message}</FormErrorMessage>
                    </FormField>
                  )} />
              </ModalContentItem>
            </ModalContent>

            <ActionArea>
              <ActionAreaButton type="submit">
                액션
              </ActionAreaButton>
            </ActionArea>
          </FlexBox>
        </ModalContainer>
      </Modal>

      <Button onClick={() => setShow(true)}>Click me</Button>
    </>
  )
}

export default Demo;`}
/>

### Hide Close Button

`TopNavigation` 에 `rightButton={null}` 으로 사용합니다.

<Demo code={`import { useState } from 'react';
import {
  Button,
  Modal,
  ActionArea,
  ActionAreaButton,
  ModalContainer,
  ModalContent,
  ModalContentItem,
  ModalDescription,
  ModalHeading,
  TopNavigation,
  ModalSummary,
  TopNavigationButton,
  ModalClose
} from '@wanteddev/wds';
import { IconChevronLeft } from '@wanteddev/wds-icon';

const Demo = () => {
  const [show, setShow] = useState(false);

  return (
    <>
      <Modal
        open={show}
        onOpenChange={setShow}
      >
        <ModalContainer variant="popup">
          <TopNavigation rightButton={null}>
            제목
          </TopNavigation>

          <ModalContent>
            <ModalContentItem>
              <ModalHeading>Heading</ModalHeading>
              <ModalSummary>Summary</ModalSummary>
              <ModalDescription>Description</ModalDescription>
            </ModalContentItem>
          </ModalContent>

          <ActionArea>
            <ActionAreaButton>
              액션
            </ActionAreaButton>
          </ActionArea>
        </ModalContainer>
      </Modal>

      <Button onClick={() => setShow(true)}>Click me</Button>
    </>
  )
}

export default Demo;`}
/>
