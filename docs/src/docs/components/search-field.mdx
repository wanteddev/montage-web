---
title: SearchField
description: SearchField Component
---

# SearchField

사용자가 컨텐츠를 검색을 할 수 있는 요소를 제공합니다.

## 활용

<Demo code={`import { SearchField } from '@wanteddev/wds';

const Demo = () => {
  return (
    <SearchField placeholder="검색어를 입력해주세요." width="300px" />
  )
}

export default Demo;`}
/>

## Props

### SearchField

<PropsTable component="SearchField" />

## Example

### Status

disabled, readOnly 상태는 아래와 같이 렌더링됩니다.

<Demo code={`import { SearchField, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <SearchField
        placeholder="readOnly"
        width="300px"
        readOnly
      />

      <SearchField
        placeholder="readOnly"
        value="readOnly, active"
        width="300px"
        readOnly
      />

      <SearchField
        placeholder="disabled"
        disabled
        width="300px"
      />

      <SearchField
        placeholder="disabled"
        value="disabled, active"
        disabled
        width="300px"
      />
    </FlexBox>
  )
}

export default Demo;`}
/>

### Size

<Demo code={`import { SearchField, FlexBox } from '@wanteddev/wds';

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="20px">
      <SearchField
        placeholder="검색어를 입력해주세요. Medium"
        width="300px"
        size="medium"
      />

      <SearchField
        placeholder="검색어를 입력해주세요. Small"
        width="300px"
        size="small"
      />
    </FlexBox>
  )
}

export default Demo;`}
/>
