---
title: useSnackbar
description: useSnackbar for WDS Utility
---

# useSnackbar

스낵바는 프로세스에 대한 간단한 메시지를 제공하며 이를 제어 가능합니다.

`RegionConfig` 컴포넌트를 활용하여 노출되는 위치를 조정할 수 있습니다.

## 활용

- 사용자가 한 행동에 대한 피드백을 화면 하단에 보여줍니다.
- 프로세스에 대한 간단한 메시지와 별도의 버튼을 제공합니다.
- 버튼으로 사용자와 상호작용할 수 있습니다.
- \[X\] 아이콘 버튼은 필요에 따라 노출하며, 버튼을 누르면 화면에서 즉시 사라집니다.
- 가독성을 위해 메시지가 2줄 이상 넘지 않도록 작성합니다.

<Demo code={`import { useSnackbar, Button } from '@wanteddev/wds';

const Demo = () => {
  const snackbar = useSnackbar();

  const handleClick = () => {
    snackbar({
      variant: 'info',
      content: '메시지에 마침표를 찍어요.',
    });
  };

  return (
    <Button onClick={handleClick}>클릭</Button>
  )
}

export default Demo;`}
/>

## Props

<PropsTable component="useSnackbar" fallback={[
{ name: 'content', types: 'string', required: true },
{ name: 'duration', types: 'number | undefined', defaultValue: 5000 },
{ name: 'variant', types: '\"normal\" | \"success\" | \"error\" | \"info\" | undefined', defaultValue: 'normal' },
{ name: 'showCloseIcon', types: 'boolean | undefined', defaultValue: 'true' },
{ name: 'action', types: 'ComponentProps<typeof TextButton> | undefined' },
]} />
