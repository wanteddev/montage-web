import {
  Meta,
  Story,
  Canvas,
  Controls,
  Primary,
  Markdown,
} from '@storybook/blocks';
import * as Stories from './index.stories';

<Meta of={Stories} />

# Thumbnail

`ImageLoader` 기반으로 해당 props를 모두 사용할 수 있습니다.

<Primary />

<Controls />

## Ratio

이미지의 비율을 설정합니다. `portrait=true` 인 경우 가로 세로 비율이 반전됩니다.

### 1:1

<Canvas>
  <Story of={Stories.OneOne} />
</Canvas>

### 4:3

<Canvas>
  <Story of={Stories.FourThree} />
</Canvas>

### Fallback

이미지 로드 중 오류가 발생한 경우 기본적으로 아래 이미지가 출력됩니다.

<Canvas>
  <Story of={Stories.Fallback} />
</Canvas>

## Responsive

가능한 옵션: `'ratio' | 'portrait' | 'css'`

`xs`, `sm`, `md`, `lg` 를 이용해 반응형 대응을 할 수 있습니다.
