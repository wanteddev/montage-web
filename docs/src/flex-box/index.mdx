import {
  Meta,
  Story,
  Canvas,
  Controls,
  Primary,
  Markdown,
} from '@storybook/blocks';
import * as Stories from './index.stories';

<Meta of={Stories} />

# Flex Box

<br/>

자주 사용하는 `display: flex;` 레이아웃을 컴포넌트화 하였습니다.

<Controls />

