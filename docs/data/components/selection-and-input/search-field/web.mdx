---
title: Search field
description: 사용자가 컨텐츠를 검색을 할 수 있는 요소를 제공합니다.
createdAt: 2025-05-12
---


## Basic search field

`SearchField` 는 주로 상단 영역에 컨텐츠를 검색할 수 있는 요소로 사용합니다.

2가지 variant를 사용할 수 있습니다.
- solid (default)
- outlined

<Demo code={`import { FlexBox, SearchField } from '@montage-ui/core';

const Demo = () => {
  return (
    <FlexBox gap="12px" flexDirection="column" alignItems="center">
      <SearchField variant="solid" width="25ch" placeholder="Solid" />
      <SearchField variant="outlined" width="25ch" placeholder="Outlined" />
    </FlexBox>
  )
}

export default Demo;`}
/>

## Sizes

2가지 크기를 제공합니다.
- medium
- large (default)

<Demo code={`import { FlexBox, SearchField, Typography } from '@montage-ui/core';

const Demo = () => {
  return (
    <FlexBox gap="16px" flexDirection="column" alignItems="center">
      <FlexBox gap="12px" alignItems="center">
        <Typography variant="body2-reading" sx={{ width: '60px' }}>Solid</Typography>

        <FlexBox gap="12px" flexDirection="column">
          <SearchField variant="solid" size="medium" width="25ch" placeholder="Medium" />
          <SearchField variant="solid" size="large" width="25ch" placeholder="Large" />
        </FlexBox>
      </FlexBox>

       <FlexBox gap="12px" alignItems="center">
        <Typography variant="body2-reading" sx={{ width: '60px' }}>Outlined</Typography>

        <FlexBox gap="12px" flexDirection="column">
          <SearchField variant="outlined" size="medium" width="25ch" placeholder="Medium" />
          <SearchField variant="outlined" size="large" width="25ch" placeholder="Large" />
        </FlexBox>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

## States

validation 을 통해 여러 상태를 표현할 수 있습니다.
- default
- disabled

<Demo code={`import { FlexBox, SearchField, Typography } from '@montage-ui/core';

const Demo = () => {
  return (
    <FlexBox gap="16px" flexDirection="column" alignItems="center">
      <FlexBox gap="12px" alignItems="center">
        <Typography variant="body2-reading" sx={{ width: '60px' }}>Solid</Typography>

        <FlexBox gap="12px" flexDirection="column">
          <SearchField variant="solid" width="25ch" placeholder="Default" />
          <SearchField variant="solid" width="25ch" placeholder="Disabled" disabled />
        </FlexBox>
      </FlexBox>

       <FlexBox gap="12px" alignItems="center">
        <Typography variant="body2-reading" sx={{ width: '60px' }}>Outlined</Typography>

        <FlexBox gap="12px" flexDirection="column">
          <SearchField variant="outlined" width="25ch" placeholder="Default" />
          <SearchField variant="outlined" width="25ch" placeholder="Disabled" disabled />
        </FlexBox>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

## Controlled

기본적으로 비제어 컴포넌트로 동작합니다.

`value`, `onChange` prop 을 사용하면 제어 컴포넌트로 동작합니다.

> 제어 컴포넌트와 비제어 컴포넌트는 [React 공식 문서](https://react.dev/learn/sharing-state-between-components#controlled-and-uncontrolled-components) 를 참조해주세요.

<Demo code={`import { FlexBox, SearchField } from '@montage-ui/core';
import { useState } from 'react';

const Demo = () => {
  const [value, setValue] = useState('');

  return (
    <FlexBox alignItems="center" flexDirection="column" gap="12px">
      <SearchField
        defaultValue="Uncontrolled"
        placeholder="Uncontrolled"
        width="25ch"
      />
      <SearchField
        value={value}
        onChange={(e) => setValue(e.target.value)}
        placeholder="Controlled"
        width="25ch"
      />
    </FlexBox>
  )
}

export default Demo;`}
/>

## API

정해진 viewport에 따라 아래 옵션을 override 할 수 있습니다.
- size

<PropsTable component="SearchField" />
