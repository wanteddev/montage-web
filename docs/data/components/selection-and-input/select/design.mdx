---
title: Select
description: 사용자가 미리 정의된 옵션 목록 중 하나 혹은 하나 이상의 항목을 선택할 수 있는 입력 요소입니다.
image: /components/selection-and-input/select/design/Thumbnail.png
createdAt: 2025-08-11
---

<Anatomy
  src="/components/selection-and-input/select/design/Image.png"
  ratio="21:9"
  data={[
    'Heading',
    'Required badge',
    'Leading icon',
    'Dropdown icon',
    'Placeholder',
    'Description',
    'Container'
  ]}
/>

<SectionVariants
  components={['FormField', 'FormControl', 'FormMessage', 'FormLabel', 'Select', 'SelectContent', 'SelectMultiple', 'Option', 'ChipAction']}
  icons={['IconGlobe', 'IconCloseThick']}
  states={`
    const [value, setValue] = React.useState([]);
    const ref = React.useRef(null);
  `}
  render={(value) => {
    const props = {};
    const component = value['Variants'] === 'Single' ? 'Select' : 'SelectMultiple';

    let render = undefined;
    let leadingContent = undefined;

    let optionVariant = 'radio';

    if (component === 'SelectMultiple') {
      props.overflow = value['Overflow'] === 'True';
      optionVariant = 'checkbox';

      if (value['Render'] === 'Chip') {
        render = `() => (
          value.map((v) => (
            <ChipAction
              key={v}
              size="xsmall"
              variant="solid"
              trailingContent={<IconCloseThick />}
              onClick={(e) => {
                e.stopPropagation();
                setValue(value.filter(data => data !== v))
                ref.current?.focus();
              }}
            >
              {v}
            </ChipAction>
          ))
        )`;
      }
    }

    if (value['Leading icon'] === 'True') {
      leadingContent = '<SelectContent variant="icon"><IconGlobe /></SelectContent>';
    }

    return `
      <FormField sx={{ width: '75%' }}>
        <FormLabel required>Heading</FormLabel>
        <FormControl>
          <${component}
            width="100%"
            ref={ref}
            placeholder="Placeholder"
            leadingContent={${leadingContent}}
            {...${JSON.stringify(props)}}
            ${component === 'SelectMultiple' ? `onChange={setValue} value={value} render={${render}}` : ''}
          >
            <Option value="Option 1" variant="${optionVariant}">Option 1</Option>
            <Option value="Option 2" variant="${optionVariant}">Option 2</Option>
            <Option value="Option 3" variant="${optionVariant}">Option 3</Option>
            <Option value="Option 4" variant="${optionVariant}">Option 4</Option>
            <Option value="Option 5" variant="${optionVariant}">Option 5</Option>
          </${component}>
        </FormControl>
        <FormMessage>Description</FormMessage>
      </FormField>
    `;
  }}
  variants={[
    {
      key: 'Variants',
      options: [
        { label: 'Single', value: {} },
        { label: 'Multiple', value: {} },
      ]
    },
    {
      key: 'Leading icon',
      options: [
        { label: 'False', value: {} },
        { label: 'True', value: {} },
      ],
    },
    {
      key: 'Render',
      disabled: (value) => value['Variants'] === 'Single',
      options: [
        { label: 'Text', value: {} },
        { label: 'Chip', value: {} },
      ],
    },
    {
      key: 'Overflow',
      disabled: (value) => value['Variants'] === 'Single',
      options: [
        { label: 'False', value: {} },
        { label: 'True', value: {} },
      ]
    },
  ]}
/>

<SectionStates
  src="/components/selection-and-input/select/design/Image-1.png"
  ratio="16:9"
  options={['Normal', 'Focused', 'Negative', 'Disabled']}
/>

<SectionLayout title="Triggerable Components">
  <SectionFigureGroup title="Menu">
    <SectionFigure
      title="Explanation"
      ratio="21:9"
      description="Select의 기본 호출 요소로 웹에서만 호출이 가능하며 단일, 복수 선택이 가능합니다."
      src="/components/selection-and-input/select/design/Image-2.png"
    />
  </SectionFigureGroup>
  <SectionFigureGroup title="Modal">
    <SectionFigure
      title="Explanation"
      ratio="16:9"
      description="Bottomsheet, Popup과 같은 모든 형태의 모달을 호출할 수 있으며 모바일에서는 메뉴 대신 바텀시트를 호출합니다. 이 외에도 모바일에서 상세한 필터를 제공할 때에는 Bottomsheet, 데스크탑 환경에서는 Popup 형태의 모달을 활용할 수 있습니다."
      src="/components/selection-and-input/select/design/Image-3.png"
    />
  </SectionFigureGroup>
</SectionLayout>

<SectionLayout title="Render">
  <SectionFigure
    title="Explanation"
    ratio="16:9"
    description="Render 요소는 Text와 Chip 두가지로 이루어져 있습니다. Text는 단일, 복수의 값을 모두 표현할 수 있으며 Chip은 복수의 값을 표현할 때만 사용 권장하고 있습니다."
    src="/components/selection-and-input/select/design/Image-4.png"
  />
</SectionLayout>

<SectionLayout title="Overflow">
  <SectionFigure
    title="Explanation"
    ratio="21:9"
    description="Overflow는 False가 기본값이며 중요하게 관리 및 컨트롤이 필요한 값들을 관리에 용이하도록 별도 스크롤이나 액션 없이 필드 내에서 한 눈에 볼 수 있도록 돕는 옵션입니다."
    src="/components/selection-and-input/select/design/Image-5.png"
  />
</SectionLayout>

<SectionLayout title="Gap">
  <SectionFigure
    title="Explanation"
    ratio="21:9"
    description="필드간의 간격은 기본적으로 Description의 활성 여부와 관계없이 동일한 간격을 유지합니다.
단, 레이아웃이 제한적인 상황일 시 필드가 고정되어 위치하여 Description만큼 간격이 줄어들 수 있습니다.
간격은 시스템 자체에서 제공되지 않으며, 프로젝트 담당 개발자와 디자이너의 논의 하에 결정할 수 있습니다."
    src="/components/selection-and-input/select/design/Image-6.png"
  />
</SectionLayout>

<SectionLayout title="Usage">
  <SectionFigureGroup title="Single selection">
    <SectionFigure
      title="Explanation"
      ratio="21:9"
      description="복수 선택 요소와 함께 사용할 때는 Radio를 권장하지만 별도 버튼을 누르지 않고도 값이 입력될 시 Check icon과 함께 자유롭게 사용할 수 있습니다."
      src="/components/selection-and-input/select/design/Image-7.png"
    />
  </SectionFigureGroup>
  <SectionFigureGroup title="Multiple selection">
    <SectionFigure
      title="Explanation"
      ratio="21:9"
      description="복수 선택일 경우 반드시 유저가 인지할 수 있어야하기 때문에 Menu에서 Checkbox를 사용을 권장합니다. 필드 내의 Render 타입은 자유롭게 사용할 수 있지만 가급적 Chip 형태 사용을 권장합니다."
      src="/components/selection-and-input/select/design/Image-8.png"
    />
  </SectionFigureGroup>
</SectionLayout>

<SectionLayout title="With search">
  <SectionFigure
    title="Usage"
    ratio="21:9"
    description="항목이 많을 경우 검색 요소를 배치하여 유저에게 탐색의 편의성을 제공합니다. 해당 스펙은 시스템으로 반영 되어있지 않습니다."
    src="/components/selection-and-input/select/design/Image-9.png"
  />
</SectionLayout>

{/* 
<SectionLayout title="How to use">
  <SectionFigure
    variant="positive"
    ratio="21:9"
    description="사유를 명확하게 안내하여 사용자에게 명확한 행동을 유도합니다."
    src="/components/selection-and-input/select/design/Image-10.png"
  />
  <SectionFigure
    variant="negative"
    ratio="21:9"
    description="폰트 사이즈를 변형하거나 Dropdown Icon을 변경하여 사용하지 않습니다."
    src="/components/selection-and-input/filter-chip/design/Image-10.png"
  />
</SectionLayout> */}
