---
title: Push badge
createdAt: 2025-09-08
---

## Variants

PushBadge는 3가지 variant를 제공합니다.
- dot
- text
- max-count

### Dot

<Demo code={`import { Avatar, PushBadge, FlexBox, IconButton, Switch } from '@montage-ui/core';
import { IconBell } from '@montage-ui/icon';
import { useState } from 'react';

const Demo = () => {
  const [invisible, setInvisible] = useState(false);

  return (
    <FlexBox flexDirection="column" gap="8px">
      <Switch checked={!invisible} onCheckedChange={(value) => setInvisible(!value)} size="small" />

      <FlexBox gap="16px" alignItems="center">
        <PushBadge invisible={invisible}>
          <IconButton>
            <IconBell />
          </IconButton>
        </PushBadge>

        <PushBadge invisible={invisible}>
          <Avatar
            src="https://static.wanted.co.kr/oneid-user/2kcR2ULXRgEC7Rgk5psLTK/BwWtS7ZW8ryS2dpQrAW8Cr.png"
            alt="User"
          />
        </PushBadge>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

### Text

<Demo code={`import { Avatar, PushBadge, FlexBox, IconButton, Switch } from '@montage-ui/core';
import { IconBell } from '@montage-ui/icon';
import { useState } from 'react';

const Demo = () => {
  const [invisible, setInvisible] = useState(false);

  return (
    <FlexBox flexDirection="column" gap="8px">
      <Switch checked={!invisible} onCheckedChange={(value) => setInvisible(!value)} size="small" />

      <FlexBox gap="16px" alignItems="center">
        <PushBadge variant="text" text="N" invisible={invisible}>
          <IconButton>
            <IconBell />
          </IconButton>
        </PushBadge>

        <PushBadge variant="text" text={3} invisible={invisible}>
          <IconButton>
            <IconBell />
          </IconButton>
        </PushBadge>

        <PushBadge variant="text" text="N" invisible={invisible}>
          <Avatar
            src="https://static.wanted.co.kr/oneid-user/2kcR2ULXRgEC7Rgk5psLTK/BwWtS7ZW8ryS2dpQrAW8Cr.png"
            alt="User"
          />
        </PushBadge>

        <PushBadge variant="text" text={3} invisible={invisible}>
          <Avatar
            src="https://static.wanted.co.kr/oneid-user/2kcR2ULXRgEC7Rgk5psLTK/BwWtS7ZW8ryS2dpQrAW8Cr.png"
            alt="User"
          />
        </PushBadge>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

### Max count

`text` 옵션에 숫자 상한치를 적용합니다. `maxCount` 옵션을 주어 최대 허용 숫자를 지정할 수 있어요.

<Demo code={`import { Avatar, PushBadge, FlexBox, IconButton, Switch } from '@montage-ui/core';
import { IconBell } from '@montage-ui/icon';
import { useState } from 'react';

const Demo = () => {
  const [invisible, setInvisible] = useState(false);

  return (
    <FlexBox flexDirection="column" gap="8px">
      <Switch checked={!invisible} onCheckedChange={(value) => setInvisible(!value)} size="small" />

      <FlexBox gap="16px" alignItems="center">
        <PushBadge variant="max-count" text={100} invisible={invisible}>
          <IconButton>
            <IconBell />
          </IconButton>
        </PushBadge>

        <PushBadge variant="max-count" text={1000} maxCount={999} invisible={invisible}>
          <Avatar
            src="https://static.wanted.co.kr/oneid-user/2kcR2ULXRgEC7Rgk5psLTK/BwWtS7ZW8ryS2dpQrAW8Cr.png"
            alt="User"
          />
        </PushBadge>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

## Position

- `position` 옵션으로 뱃지의 위치를 조정할 수 있습니다.
- `offsetX`, `offsetY` 를 이용하여 추가적인 위치를 조정할 수 있습니다.

<Demo code={`import { TextButton, PushBadge, FlexBox, Box } from '@montage-ui/core';

const ExampleBox = () => {
  return (
    <Box
      sx={(theme) => ({
        boxShadow: \`inset 0 0 0 1px $\{theme.semantic.line.neutral.primaryOpaque}\`,
        width: 60,
        height: 60,
      })}
    />
  )
}

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <FlexBox gap="16px" alignItems="center">
        <PushBadge position="top-left">
          <ExampleBox />
        </PushBadge>
        <PushBadge position="top-center">
          <ExampleBox />
        </PushBadge>
        <PushBadge position="top-right">
          <ExampleBox />
        </PushBadge>
      </FlexBox>

      <FlexBox gap="16px" alignItems="center">
        <PushBadge position="middle-left">
          <ExampleBox />
        </PushBadge>
        <PushBadge position="middle-center">
          <ExampleBox />
        </PushBadge>
        <PushBadge position="middle-right">
          <ExampleBox />
        </PushBadge>
      </FlexBox>

      <FlexBox gap="16px" alignItems="center">
        <PushBadge position="bottom-left">
          <ExampleBox />
        </PushBadge>
        <PushBadge position="bottom-center">
          <ExampleBox />
        </PushBadge>
        <PushBadge position="bottom-right">
          <ExampleBox />
        </PushBadge>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

## Sizes

3가지 사이즈를 제공합니다.
- xsmall (default)
- small
- medium

<Demo code={`import { PushBadge, FlexBox, Box } from '@montage-ui/core';

const ExampleBox = () => {
  return (
    <Box
      sx={(theme) => ({
        boxShadow: \`inset 0 0 0 1px $\{theme.semantic.line.neutral.primaryOpaque}\`,
        width: 60,
        height: 60,
      })}
    />
  )
}

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <FlexBox gap="16px" alignItems="center">
        <PushBadge size="xsmall" variant="dot">
          <ExampleBox />
        </PushBadge>
        <PushBadge size="small" variant="dot">
          <ExampleBox />
        </PushBadge>
        <PushBadge size="medium" variant="dot">
          <ExampleBox />
        </PushBadge>
      </FlexBox>

      <FlexBox gap="16px" alignItems="center">
        <PushBadge size="xsmall" variant="text" text="N">
          <ExampleBox />
        </PushBadge>
        <PushBadge size="small" variant="text" text="N">
          <ExampleBox />
        </PushBadge>
        <PushBadge size="medium" variant="text" text="N">
          <ExampleBox />
        </PushBadge>
      </FlexBox>

      <FlexBox gap="16px" alignItems="center">
        <PushBadge size="xsmall" variant="text" text={12}>
          <ExampleBox />
        </PushBadge>
        <PushBadge size="small" variant="text" text={12}>
          <ExampleBox />
        </PushBadge>
        <PushBadge size="medium" variant="text" text={12}>
          <ExampleBox />
        </PushBadge>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>

## Outline border

- `outlineBorder` 옵션을 사용하여 외곽에 테두리를 표시 할 수 있습니다.
- `outlineBorderColor` 옵션을 사용하면 테두리 색상을 변경할 수 있습니다.

<Demo
  defaultIsTransparent
  code={`import { PushBadge, FlexBox, Box } from '@montage-ui/core';

const ExampleBox = () => {
  return (
    <Box
      sx={(theme) => ({
        boxShadow: \`inset 0 0 0 1px $\{theme.semantic.line.neutral.primaryOpaque}\`,
        backgroundColor: theme.semantic.surface.neutral.inverse,
        width: 60,
        height: 60,
      })}
    />
  )
}

const Demo = () => {
  return (
    <FlexBox flexDirection="column" gap="8px">
      <FlexBox gap="16px" alignItems="center">
        <PushBadge outlineBorder size="xsmall" variant="dot">
          <ExampleBox />
        </PushBadge>
        <PushBadge outlineBorder size="small" variant="dot">
          <ExampleBox />
        </PushBadge>
        <PushBadge outlineBorder size="medium" variant="dot">
          <ExampleBox />
        </PushBadge>
      </FlexBox>

      <FlexBox gap="16px" alignItems="center">
        <PushBadge outlineBorder size="xsmall" variant="text" text="N">
          <ExampleBox />
        </PushBadge>
        <PushBadge outlineBorder size="small" variant="text" text="N">
          <ExampleBox />
        </PushBadge>
        <PushBadge outlineBorder size="medium" variant="text" text="N">
          <ExampleBox />
        </PushBadge>
      </FlexBox>

      <FlexBox gap="16px" alignItems="center">
        <PushBadge outlineBorder size="xsmall" variant="text" text={12}>
          <ExampleBox />
        </PushBadge>
        <PushBadge outlineBorder size="small" variant="text" text={12}>
          <ExampleBox />
        </PushBadge>
        <PushBadge outlineBorder size="medium" variant="text" text={12}>
          <ExampleBox />
        </PushBadge>
      </FlexBox>
    </FlexBox>
  )
}

export default Demo;`}
/>


## API

<PropsTable component="PushBadge" />