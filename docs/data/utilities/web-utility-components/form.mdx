---
title: Form
description: Form 컴포넌트는 form 구조 및 접근성 처리를 다루는 컴포넌트입니다.
createdAt: 2025-06-17
---

## Introduce

form 구조 및 접근성 처리를 다룹니다.
각 컴포넌트별 활용법은 `TextField`, `TextArea`, `Checkbox`, `Radio` 등을 참고해주세요.

이 문서는 `react-hook-form`과 함께 활용하는 예시를 소개합니다.

<Demo code={`import { Button, FlexBox, TextField, FormControl, FormControlField, FormControlLabel, FormControlMessage, FormControlNegativeMessage } from '@montage-ui/core';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm({
    defaultValues: {
      content: ''
    }
  });

  return (
    <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
            value: true,
            message: '필수 값입니다.'
          }
        }}
        render={({ field, formState }) => (
          <FormControl>
            <FormControlLabel>라벨</FormControlLabel>

            <FormControlField>
              <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.content)} />
            </FormControlField>

            {Boolean(formState.errors.content) ? (
              <FormControlNegativeMessage>
                {formState.errors.content?.message}
              </FormControlNegativeMessage>
            ) : (
             <FormControlMessage>값을 입력해주세요.</FormControlMessage>
            )}
          </FormControl>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}/>

## Anatomy

`FormControl` 컴포넌트는 아래와 같은 구조를 가집니다.

```tsx
<form onSubmit={handleSubmit}>
  <FormControl>
    <FormControlLabel>label</FormControlLabel>
    <FormControlField>
      <input {...field} />
    </FormControlField>

    {status === 'normal' && <FormControlMessage>설명</FormControlMessage>}
    {status === 'negative' && <FormControlNegativeMessage>오류 메시지</FormControlNegativeMessage>}
    {status === 'positive' && <FormControlPositiveMessage>성공 메시지</FormControlPositiveMessage>}
  </FormControl>

  <button type="submit" />
</form>
```

## Label placement

`labelPlacement` 를 조절하여 라벨의 위치를 조절할 수 있습니다.
- top (default)
- start

<Demo code={`import { Button, FlexBox, TextField, FormControl, FormControlField, FormControlLabel, FormControlMessage, FormControlNegativeMessage, FormControlCharacterCounter } from '@montage-ui/core';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm({
    defaultValues: {
      top: '',
      start: ''
    }
  });

  return (
    <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
      <Controller
        control={form.control}
        name="top"
        rules={{
          required: {
            value: true,
            message: '필수 값입니다.'
          }
        }}
        render={({ field, formState }) => (
          <FormControl>
            <FormControlLabel>라벨</FormControlLabel>

            <FormControlField>
              <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.top)} />
            </FormControlField>

            {Boolean(formState.errors.top) ? (
              <FormControlNegativeMessage>
                {formState.errors.top?.message}
              </FormControlNegativeMessage>
            ) : (
             <FormControlMessage>값을 입력해주세요.</FormControlMessage>
            )}
          </FormControl>
        )}
      />

      <Controller
        control={form.control}
        name="start"
        rules={{
          required: {
            value: true,
            message: '필수 값입니다.'
          }
        }}
        render={({ field, formState }) => (
          <FormControl labelPlacement="start">
            <FormControlLabel>라벨</FormControlLabel>

            <FormControlField>
              <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.start)} />
            </FormControlField>

            {Boolean(formState.errors.start) ? (
              <FormControlNegativeMessage>
                {formState.errors.start?.message}
              </FormControlNegativeMessage>
            ) : (
             <FormControlMessage>값을 입력해주세요.</FormControlMessage>
            )}
          </FormControl>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}/>



## Size

2가지 크기를 사용할 수 있습니다.

`TextField`, `DatePicker`, `DateRangePicker`, `TimePicker`의 크기가 자동으로 전파됩니다.

### Medium

<Demo code={`import { Button, FlexBox, TextField, FormControl, FormControlField, FormControlLabel, FormControlMessage, FormControlNegativeMessage, FormControlCharacterCounter } from '@montage-ui/core';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm({
    defaultValues: {
      content: ''
    }
  });

  return (
    <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
            value: true,
            message: '필수 값입니다.'
          }
        }}
        render={({ field, formState }) => (
          <FormControl size="medium">
            <FormControlLabel>라벨</FormControlLabel>

            <FormControlField>
              <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.content)} />
            </FormControlField>

            {Boolean(formState.errors.content) ? (
              <FormControlNegativeMessage>
                {formState.errors.content?.message}
              </FormControlNegativeMessage>
            ) : (
             <FormControlMessage>값을 입력해주세요.</FormControlMessage>
            )}
          </FormControl>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}/>

### Large

<Demo code={`import { Button, FlexBox, TextField, FormControl, FormControlField, FormControlLabel, FormControlMessage, FormControlNegativeMessage, FormControlCharacterCounter } from '@montage-ui/core';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm({
    defaultValues: {
      content: ''
    }
  });

  return (
    <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
            value: true,
            message: '필수 값입니다.'
          }
        }}
        render={({ field, formState }) => (
          <FormControl size="large">
            <FormControlLabel>라벨</FormControlLabel>

            <FormControlField>
              <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.content)} />
            </FormControlField>

            {Boolean(formState.errors.content) ? (
              <FormControlNegativeMessage>
                {formState.errors.content?.message}
              </FormControlNegativeMessage>
            ) : (
             <FormControlMessage>값을 입력해주세요.</FormControlMessage>
            )}
          </FormControl>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}/>


## With character counter

Message 종류 컴포넌트에 character counter를 추가할 수 있습니다.

<Demo code={`import { Button, FlexBox, TextField, FormControl, FormControlField, FormControlLabel, FormControlMessage, FormControlNegativeMessage, FormControlCharacterCounter } from '@montage-ui/core';
import { useForm, Controller } from 'react-hook-form';

const Demo = () => {
  const form = useForm({
    defaultValues: {
      content: ''
    }
  });

  return (
    <FlexBox as="form" flexDirection="column" gap="12px" onSubmit={form.handleSubmit(v => alert(JSON.stringify(v)))}>
      <Controller
        control={form.control}
        name="content"
        rules={{
          required: {
           value: true,
           message: '필수 값입니다.'
          },
          maxLength: {
            value: 20,
            message: '최대 입력가능 글자를 초과했습니다.'
          }
        }}
        render={({ field, formState }) => (
          <FormControl>
            <FormControlLabel>라벨</FormControlLabel>

            <FormControlField>
              <TextField {...field} width="300px" placeholder="값을 입력하세요." invalid={Boolean(formState.errors.content)} />
            </FormControlField>

            {Boolean(formState.errors.content) ? (
              <FormControlNegativeMessage characterCounter={<FormControlCharacterCounter length={field.value.length} maxLength={20} />}>
                {formState.errors.content?.message}
              </FormControlNegativeMessage>
            ) : (
             <FormControlMessage characterCounter={<FormControlCharacterCounter length={field.value.length} maxLength={20} />}>값을 입력해주세요.</FormControlMessage>
            )}
          </FormControl>
        )}
      />

      <Button type="submit">제출</Button>
    </FlexBox>
  )
}

export default Demo;`}/>


## Common issue

`form.reset()` 이 동작하지 않는 경우가 있습니다.
이 경우 `defaultValue` 가 설정되어 있는지 확인하세요.

[react-hook-form](https://github.com/orgs/react-hook-form/discussions/5858) 은 undefined value를 지원하지 않습니다.

## Accessibility

- Unique id를 생성하여 label의 htmlFor, input의 id를 연결합니다.
- aria-describedby, aria-labelledby를 통해 추가 메시지를 연결합니다.

## API

### FormControl

<PropsTable component="FormControl" />

### FormControlLabel

<PropsTable component="FormControlLabel" />

### FormControlField

children으로 form 요소 컴포넌트를 넣어 사용합니다.

### FormControlMessage

<PropsTable component="FormControlMessage" />

### FormControlNegativeMessage

<PropsTable component="FormControlNegativeMessage" />

### FormControlPositiveMessage

<PropsTable component="FormControlPositiveMessage" />

### FormControlCharacterCounter

<PropsTable component="FormControlCharacterCounter" />