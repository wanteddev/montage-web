// eslint-disable-next-line @typescript-eslint/no-require-imports
const { Button, css, theme } = require('@wanteddev/wds');

console.assert(Button, 'Button');
console.assert(theme, 'theme');
console.assert(css, 'css');
